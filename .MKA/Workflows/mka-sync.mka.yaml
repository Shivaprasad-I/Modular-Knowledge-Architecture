id: mka-sync
intent: "Sync the MKA index and heal broken paths automatically."
workflow_nodes:
  - file: "mka-cli/src/main.rs"
    method: "main"
    note: "Routes to the sync handler."
  - file: "mka-cli/src/commands/sync.rs"
    method: "handle"
    note: "Core synchronization and healing logic."
  - file: "mka-cli/src/commands/sync.rs"
    method: "heal_path"
    note: "Logic to find moved files relative to the project root."
validation:
  test_file: "mka-cli/src/commands/sync.rs"
