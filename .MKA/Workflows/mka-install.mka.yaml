id: mka-install
intent: "Install a Tree-sitter parser library (.so) to the centralized MKA configuration directory."
workflow_nodes:
  - file: "mka-cli/src/main.rs"
    method: "main"
  - file: "mka-cli/src/commands/install.rs"
    method: "handle"
validation:
  test_file: "mka-cli/src/commands/install.rs"
