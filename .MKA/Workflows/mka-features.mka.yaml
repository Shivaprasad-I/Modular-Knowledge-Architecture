id: mka-features
intent: "List all available workflows/features from the MKA index."
nodes:
  - file: "mka-cli/src/main.rs"
    method: "main"
    note: "Routes to the features command."
  - file: "mka-cli/src/commands/features.rs"
    method: "handle"
    note: "Reads and displays the MKA index in TOON format."
validation:
  test_file: "mka-cli/src/commands/features.rs"
