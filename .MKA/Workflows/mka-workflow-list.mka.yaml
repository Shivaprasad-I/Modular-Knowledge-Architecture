id: mka-workflow-list
intent: "List all available workflows in TOON format."
workflow_nodes:
  - file: "mka-cli/src/main.rs"
    method: "main"
    note: "Routes to the workflow-list handler."
  - file: "mka-cli/src/commands/workflow_list.rs"
    method: "handle"
    note: "Iterates through the index and prints workflows."
validation:
  test_file: "mka-cli/src/commands/workflow_list.rs"
